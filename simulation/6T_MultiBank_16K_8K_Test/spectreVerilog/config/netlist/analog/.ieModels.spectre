// BEGIN Flat Interface Elements
// Flattened IE at /~WDataAck<8> uses port path 99999
// Flattened IE at /~RData<30> uses port path 99998
// Flattened IE at /~RData<31> uses port path 99997
// Flattened IE at /~WriteEn uses port path 99996
// Flattened IE at /~WAck uses port path 99995
// Flattened IE at /~RAck uses port path 99994
// Flattened IE at /~WDataAck<15> uses port path 99993
// Flattened IE at /~R_Address<0> uses port path 99992
// Flattened IE at /~R_Address<1> uses port path 99991
// Flattened IE at /~R_Address<2> uses port path 99990
// Flattened IE at /~R_Address<3> uses port path 99989
// Flattened IE at /~RData<6> uses port path 99988
// Flattened IE at /~RData<7> uses port path 99987
// Flattened IE at /~RData<8> uses port path 99986
// Flattened IE at /~RData<9> uses port path 99985
// Flattened IE at /~RData<10> uses port path 99984
// Flattened IE at /~RData<11> uses port path 99983
// Flattened IE at /~RData<12> uses port path 99982
// Flattened IE at /~RData<19> uses port path 99981
// Flattened IE at /~RData<20> uses port path 99980
// Flattened IE at /~RData<21> uses port path 99979
// Flattened IE at /~RData<22> uses port path 99978
// Flattened IE at /~RData<5> uses port path 99977
// Flattened IE at /~WData<0> uses port path 99976
// Flattened IE at /~WData<1> uses port path 99975
// Flattened IE at /~WData<2> uses port path 99974
// Flattened IE at /~WData<3> uses port path 99973
// Flattened IE at /~WData<4> uses port path 99972
// Flattened IE at /~WData<5> uses port path 99971
// Flattened IE at /~WData<6> uses port path 99970
// Flattened IE at /~RData<2> uses port path 99969
// Flattened IE at /~RData<3> uses port path 99968
// Flattened IE at /~RData<4> uses port path 99967
// Flattened IE at /~WData<20> uses port path 99966
// Flattened IE at /~WData<19> uses port path 99965
// Flattened IE at /~WData<7> uses port path 99964
// Flattened IE at /~W_Address<9> uses port path 99963
// Flattened IE at /~W_Address<10> uses port path 99962
// Flattened IE at /~W_Address<11> uses port path 99961
// Flattened IE at /~RData<1> uses port path 99960
// Flattened IE at /~WData<18> uses port path 99959
// Flattened IE at /~RData<0> uses port path 99958
// Flattened IE at /~WData<31> uses port path 99957
// Flattened IE at /~WData<27> uses port path 99956
// Flattened IE at /~WData<28> uses port path 99955
// Flattened IE at /~WData<29> uses port path 99954
// Flattened IE at /~WData<30> uses port path 99953
// Flattened IE at /~WData<21> uses port path 99952
// Flattened IE at /~WDataAck<11> uses port path 99951
// Flattened IE at /~WDataAck<10> uses port path 99950
// Flattened IE at /~RData<24> uses port path 99949
// Flattened IE at /~WDataAck<9> uses port path 99948
// Flattened IE at /~R_Address<10> uses port path 99947
// Flattened IE at /~R_Address<9> uses port path 99946
// Flattened IE at /~RData<29> uses port path 99945
// Flattened IE at /~R_Address<8> uses port path 99944
// Flattened IE at /~R_Address<7> uses port path 99943
// Flattened IE at /~R_Address<11> uses port path 99942
// Flattened IE at /~R_Address<6> uses port path 99941
// Flattened IE at /~RData<26> uses port path 99940
// Flattened IE at /~RData<23> uses port path 99939
// Flattened IE at /~WDataAck<14> uses port path 99938
// Flattened IE at /~WDataAck<12> uses port path 99937
// Flattened IE at /~R_Address<5> uses port path 99936
// Flattened IE at /~RData<28> uses port path 99935
// Flattened IE at /~RData<27> uses port path 99934
// Flattened IE at /~RData<18> uses port path 99933
// Flattened IE at /~RData<25> uses port path 99932
// Flattened IE at /~RData<17> uses port path 99931
// Flattened IE at /~RData<16> uses port path 99930
// Flattened IE at /~RData<15> uses port path 99929
// Flattened IE at /~R_Address<4> uses port path 99928
// Flattened IE at /~RData<14> uses port path 99927
// Flattened IE at /~RData<13> uses port path 99926
// Flattened IE at /~W_Address<5> uses port path 99925
// Flattened IE at /~WData<25> uses port path 99924
// Flattened IE at /~W_Address<4> uses port path 99923
// Flattened IE at /~W_Address<3> uses port path 99922
// Flattened IE at /~WDataAck<4> uses port path 99921
// Flattened IE at /~WDataAck<5> uses port path 99920
// Flattened IE at /~WDataAck<6> uses port path 99919
// Flattened IE at /~WDataAck<7> uses port path 99918
// Flattened IE at /~WDataAck<3> uses port path 99917
// Flattened IE at /~WDataAck<2> uses port path 99916
// Flattened IE at /~WData<24> uses port path 99915
// Flattened IE at /~WDataAck<1> uses port path 99914
// Flattened IE at /~WData<23> uses port path 99913
// Flattened IE at /~WDataAck<0> uses port path 99912
// Flattened IE at /~WData<22> uses port path 99911
// Flattened IE at /~WData<26> uses port path 99910
// Flattened IE at /~W_Address<7> uses port path 99909
// Flattened IE at /~W_Address<8> uses port path 99908
// Flattened IE at /~WDataAck<13> uses port path 99907
// Flattened IE at /~ReadEn uses port path 99906
// Flattened IE at /~W_Address<0> uses port path 99905
// Flattened IE at /~W_Address<1> uses port path 99904
// Flattened IE at /~W_Address<2> uses port path 99903
// Flattened IE at /~W_Address<6> uses port path 99902
// Flattened IE at /~WData<8> uses port path 99901
// Flattened IE at /~WData<9> uses port path 99900
// Flattened IE at /~WData<10> uses port path 99899
// Flattened IE at /~WData<14> uses port path 99898
// Flattened IE at /~WData<13> uses port path 99897
// Flattened IE at /~WData<12> uses port path 99896
// Flattened IE at /~WData<11> uses port path 99895
// Flattened IE at /~WData<17> uses port path 99894
// Flattened IE at /~WData<16> uses port path 99893
// Flattened IE at /~WData<15> uses port path 99892
// END Flat Interface Elements
